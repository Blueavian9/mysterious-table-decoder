/**
 * Decodes a message from the given data and returns it in base64 format
 * @param {Object} data - The input data object
 * @param {string} data.table - The table of characters
 * @param {number[]} data.indices - Array of indices into the table
 * @returns {string} - The decoded message in base64 format
 */
function decode(data) {
    // Your solution here
}

module.exports = decode; 